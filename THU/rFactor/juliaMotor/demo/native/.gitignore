# per-run telemetry logs
zand_racer_*.txt
# legacy-named logs from before the Zand Racer rename
sand_racer_*.txt
# joystick calibration is machine-specific
joystick.conf
joystick_profiles/
# prebuilt sysimage (large, machine-specific — build via build_sysimage.jl)
jlracer.so
# per-session race state (the GUI's post-race screen + AI-% preset read these; not source)
last_race_result.txt
human_best.txt
