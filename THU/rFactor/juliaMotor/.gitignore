
# iRacing .ibt telemetry (large binaries, personal copies)
data/iracing/*.ibt
# juliaMotor-emitted .ibt telemetry (JM_IBT) — same format, for iRacing comparison
data/juliaracer/*.ibt
# iRacing .ibt also in dated subfolders (large personal binaries)
data/iracing/**/*.ibt
# generated engine-sound preview (regenerate via demo/native/sound/preview_engine.jl)
demo/native/sound/lotus49_rev_preview.wav
# python bytecode
__pycache__/
*.pyc

# generated replay recordings
data/juliaracer/*.jmr
*.jmr

# runtime: human lap-time stash for AI % preset
human_recent.txt
human_best.txt
demo/native/human_recent.txt
demo/native/human_best.txt

# Per-run telemetry dumps written next to the sim (demo/native/<track>_racer_<epoch>.txt).
# They are RUN ARTEFACTS, not source: 21 of them were swept into the repo by `git add -A`, three
# of those in a commit whose message was about something else entirely (3a3557f).
demo/native/*_racer_*.txt
