# bridgeIQ status — 2026-06-22

Branch **24.04** (tracks `origin/24.04`). Run with `./run.sh` (venv python).
This session delivered book-grounded context help, a full Q-Plus compliance
pass, and a long run of screenshot-driven UI fixes. All work is no-peek-safe.

## Highlights this session

### Book-grounded, no-peek context help
- Hint button and the Instrumentation (teaching) view now show a short,
  situation-specific coaching note (bidding system, vulnerability, this deal's
  visible/inferable cards) — no citations, rendered from live board state.
- Whole-deal 13-trick plan for declarer/defender, shown at start of play.
- "Ask Claude" for deeper, book-grounded advice — visible but disabled unless
  Claude is enabled in Preferences → AI & Network (offers a jump to Prefs).
- Advice is written as comments into the active `.BDL` and the `.qss`.
- **Invariant honoured everywhere: NO PEEKING.** Hidden hands are read only
  through `known_layout`/inference, never `board.hands`; plans return `None`
  rather than peek when they lack legitimately face-up cards.

### Q-Plus compliance pass
- Menu wording/placement aligned; Deal Filter made functional; Previous Deal
  full setup; Concede in the Claim dialog; Evaluate (conventional meaning /
  expected tricks); honour scoring method + play tournament-file deals;
  Results-of-file comparison; a proper MiniBridge auction-replacement engine;
  enforced + verified One-Player non-peek (bidding included).
- Networking: biq↔biq verified. (Q-Plus-client-over-Q-NET path remains the
  separate, additive track — see CLAUDE.md.)
- Full user-facing **BEN → BridgeIQ** rename sweep.

### Closed room / score table
- Closed room plays the 4-bot table in parallel with the human (Q-Plus
  semantics), now as a **continuous series** so "Next deal" plays the next one.
- Closed-room score table gained **Compare (open vs closed)** + **Replay**
  buttons (was Export/Close only); clicking a contract cell still opens that
  board's detailed proceedings.

### Instrumentation view
- Each seat's box now shows its **original 13-card shape** (♠a ♥b ♦c ♣d) at the
  lower-right, revealed as soon as it's known — exact for face-up hands,
  progressively for hidden ones — so a defender can read counts and signal.
- Exact suit length when the rest is determined (no spurious "0-1" ranges);
  shows the actual outstanding E-or-W card, not just a range.
- Blue diamond symbol to match the cards; 0-length suit reads "void".

### Other UI fixes (screenshot-driven)
- Claim: actually finalizes + scores the hand; Claim button enabled during
  play; end-of-hand overlay dismissed on repeat/next deal.
- Bidding view: table inset so the West label isn't clipped, with the inset
  applied only to the play row (not the bottom hand) and reset for cardplay.
- Claude hint: Cancel button that kills the subprocess, hardened rendering,
  model updated to Claude Opus 4.8; says "Claude" (not "Opus 4.7").
- Claude-off notice: fixed ragged text (widened labels, flush-left).
- Startup hardening: on-screen/primary-screen placement, bring-to-front,
  self-healing single-instance guard.

## Verification
- `QT_QPA_PLATFORM=offscreen PYTHONPATH=. ../venv/bin/python test_teaching_view.py`
  — all teaching-view tests pass.
- No-peek audit: `original_shape()` / help paths read hidden seats only through
  `layout`; confirmed by a peek-guard smoke test.

## Open / deferred
- Add a **Closed Room** item to the in-game Deal menu (currently only on the
  opening toolbar).
- Full score-table button parity (Save / Print / Edit Names / Export HTML) on
  the closed-room dialog if wanted — only Compare/Replay added so far.
- Q-NET server path (Q-Plus client joining a biq-hosted match) — see CLAUDE.md.
