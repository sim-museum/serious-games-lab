GUI Harness for Q-plus Bridge hand entry automation.

The venv is created automatically by install.sh (or scripts/install_dependencies.sh).

To use manually:
  cd MON/guiHarness
  source venv/bin/activate
  python bridge_input.py

The harness terminal also opens automatically when launching qplus.sh.
